language: rust
script:
  - cd choose
  - cargo build --verbose
  - cargo test --verbose
  - cd ../ddg
  - cargo build --verbose
  - cargo test --verbose
  - cd ../presence
  - cargo build --verbose
  - cargo test --verbose
  - cd ../puppet
  - cargo build --verbose
  - cargo test --verbose
  - cd ../sys
  - cargo build --verbose
  - cargo test --verbose
  - cd ../users
  - cargo build --verbose
  - cargo test --verbose
  - cd ..
notifications:
  email:
    on_success: never
  irc:
    channels:
      - "irc.fyrechat.net#vana-commits"
    template:
      - "%{repository_slug}/%{branch} (%{commit} - %{author}): %{message}"
    skip_join: true
